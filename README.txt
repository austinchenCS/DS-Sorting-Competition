Compile Instructions:
Compile with the exact command: “g++ -O3 -std=c++11 main.cpp DSString.cpp -o sorter.out”.

Execution Instructions:
The input file must be within the same containing folder as the .cpp file.

Calling the .out file must include the following command line arguments:

./sorter.out <input.file> <output.file>

Ex: “./sorter.out input_small.txt output_small.txt”

